import { Input, LoadingCard, TopBar } from '@/components'
import React, { useRef } from 'react'

function Home() {
  // const ref = useRef();

  return (
    <div>
      <TopBar />
    </div>
  )
}

export default Home
